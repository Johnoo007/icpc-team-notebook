#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using pii = pair<int, int>;
using pipii = pair<int, pii>; 
using tiii = tuple<int, int, int>; 

#define all(x) (x).begin(), (x).end()
#define fastio() ios::sync_with_stdio(0); cin.tie(0);

int solve(int quest_number) {
    vector<int> ai;
    vector<int> bi;
}

int main() {
    fastio();
    int quest_number;
    cin >> quest_number;

    for(int i=0;i<quest_number;i++){
        solve(quest_number);
    }
    

    return 0;
}